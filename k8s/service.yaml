apiVersion: v1
kind: Service
metadata:
  name: mobackend-service
spec:
  selector:
    app: mobackend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000  # Порт на ноде
  type: NodePort
